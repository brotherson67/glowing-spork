const User = require("./User");
const Thought = require("./Thought");
const Activity = require("./Activity"); // Not sure if we really need this one but exporting just in case

module.exports = { User, Thought, Activity };
